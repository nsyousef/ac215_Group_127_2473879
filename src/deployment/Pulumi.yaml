name: pibu-ai-deployment
description: Deploy inference service (GKE/Kubernetes) and LLM service (Modal) for pibu.ai
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: python
      project: pibu-ai
      infrastructure: kubernetes
      platform: gke
